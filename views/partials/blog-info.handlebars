<article class="post">
    <div class="blog-title">
        <span style="font-weight: bold; margin: 10px"><a href="/blog/{{id}}">{{title}}</a></span><span style="float:right">Posted by: {{user.username}} on {{format_date created_at}}</span>
    </div>
    <div class="blog-content">
        <p style="margin: 10px">{{content}}</p>
    <span style="float:left">{{comments.length}} {{format_plural "comment" comments.length}}</span>
    </div>
</article>